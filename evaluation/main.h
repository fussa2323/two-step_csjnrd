#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <list>
#include <vector>

std::vector<std::string> split(std::string str, std::string delim);
void dp(std::vector<std::string>, std::vector<std::string>);
